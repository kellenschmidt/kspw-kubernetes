# Oauth client configuration specifics  
config:  
  # OAuth client ID  
  clientID: "28cdd1b6b0a60cdbe0e9"  
  # OAuth client secret  
  clientSecret: "797d7134d58d18439dd43a9c3a4b3fc9829b6b10"
  # Create a new secret with the following command  
  # python -c 'import os,base64; print base64.b64encode(os.urandom(16))'  
  cookieSecret: "Z0o4SlRqOTBKVzhXZVhRQzZMUkRVQT09"  
  # Custom configuration file see https://github.com/bitly/oauth2_proxy/blob/master/contrib/oauth2_proxy.cfg.example
  configFile: |-  
    ## Pass OAuth Access token to upstream via "X-Forwarded-Access-Token"
    pass_access_token = true
    #upstreams = [  
    #    "https://kubernetes-dashboard:8443/"  
    #]  

extraArgs:  
  provider: "github"  
  # limit access to members of a github organisation:
  github-org: "kellenforthewin"
  # email-domain: "*"  
  upstream: "file:///dev/null"
  http-address: "0.0.0.0:4180"  

ingress:  
  enabled: true
  path: /oauth2  
  hosts:  
  - k8s.kellenforthe.win  
  annotations:  
    kubernetes.io/ingress.class: nginx  
    certmanager.k8s.io/issuer: letsencrypt
  tls:  
  # certmanager will use the letsencrypt issues to create the TLS secret in the namespace if they don't exist
  - secretName: certificate-k8s-dashboard
    hosts:  
    - k8s.kellenforthe.win
