apiVersion: v1
kind: Service
metadata:
  name: slim
spec:
  selector:
    app: url-shortener
    type: api
  ports:
    - name: http
      protocol: 'TCP'
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slim
spec:
  selector:
    matchLabels:
      app: url-shortener
      type: api
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: url-shortener
        type: api
    spec:
      containers:
      - name: slimphp
# Set version here
        image: kellenschmidt/kspw-slimphp:latest
        imagePullPolicy: Always
        env:
        - name: MYSQL_HOST
          value: "mysql:3306"
        envFrom:
        - secretRef:
            name: mysql-login
        - secretRef:
            name: jwt
        ports:
        - containerPort: 80
